---
description: 
globs: 
alwaysApply: false
---
# Gradle 构建配置

本项目使用 Gradle 进行构建。主要的构建逻辑和依赖项在根目录下的 [build.gradle](mdc:build.gradle) 文件中配置。

## 主要特点

*   **依赖管理**: 使用 `ConfigUtils` 类来动态管理插件和依赖项。
*   **仓库配置**: 项目配置了 `mavenLocal()`、`jitpack.io` 以及阿里云的 Maven 镜像源 (`maven.aliyun.com`) 来加速依赖下载。标准的 `google()` 和 `mavenCentral()` 仓库被注释掉了。
*   **依赖版本控制**: 使用 `resolutionStrategy` 统一管理 `com.android.support` 库的版本。
*   **自定义任务**: 包含一个自定义的 `clean` 任务，用于删除 `rootProject.buildDir`。

